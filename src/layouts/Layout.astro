---
import '../styles/global.css';
import { ViewTransitions } from 'astro:transitions';

const lang = 'en';

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const {
  title,
  description = 'NetSci Dresden 2027 — International Conference on Network Science at TU Dresden',
  image = '/tu-dresden.jpeg',
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site ?? 'https://netsci-dresden.de');
const siteOrigin = (Astro.site ?? new URL('https://netsci-dresden.de')).origin;

const eventSchema = {
  '@context': 'https://schema.org',
  '@type': 'Event',
  name: 'NetSci Dresden 2027 — International School and Conference on Network Science',
  description,
  startDate: '2027-05-17',
  endDate: '2027-05-21',
  eventAttendanceMode: 'https://schema.org/OfflineEventAttendanceMode',
  eventStatus: 'https://schema.org/EventScheduled',
  url: siteOrigin,
  image: new URL(image, siteOrigin).href,
  location: {
    '@type': 'Place',
    name: 'TU Dresden',
    address: {
      '@type': 'PostalAddress',
      streetAddress: 'Helmholtzstraße 10',
      postalCode: '01069',
      addressLocality: 'Dresden',
      addressCountry: 'DE',
    },
  },
  organizer: {
    '@type': 'Organization',
    name: 'Network Science Society',
    url: 'https://netscisociety.net',
  },
};
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <!-- Fonts: preconnect + non-blocking load -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,400&family=Zilla+Slab:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap"
    />

    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph -->
    <meta property="og:type"        content="website" />
    <meta property="og:url"         content={canonicalURL} />
    <meta property="og:title"       content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image"       content={image} />

    <!-- Twitter Card -->
    <meta name="twitter:card"        content="summary_large_image" />
    <meta name="twitter:title"       content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image"       content={image} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="theme-color" content="#001A33" />

    <!-- Structured data for rich conference results -->
    <script type="application/ld+json" set:html={JSON.stringify(eventSchema)} is:inline />

    <!-- SPA-style page transitions -->
    <ViewTransitions />
  </head>
  <body>
    <slot />

    <script>
      // Scroll-reveal via IntersectionObserver. Runs on first load and after each
      // view transition (astro:page-load fires on both).
      function initReveal() {
        const els = document.querySelectorAll('[data-reveal]:not(.is-revealed)');
        if (!els.length || !('IntersectionObserver' in window)) {
          els.forEach((el) => el.classList.add('is-revealed'));
          return;
        }
        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-revealed');
                io.unobserve(entry.target);
              }
            });
          },
          { rootMargin: '0px 0px -10% 0px', threshold: 0.12 }
        );
        els.forEach((el) => io.observe(el));
      }
      document.addEventListener('astro:page-load', initReveal);
      initReveal();

      // Landing-only demo: stop dead links (href="#") from jumping to the top.
      document.addEventListener('click', (e) => {
        const target = e.target as Element | null;
        const dead = target?.closest?.('a[href="#"]');
        if (dead) e.preventDefault();
      });
    </script>
  </body>
</html>
